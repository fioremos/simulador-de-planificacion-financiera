name: Test PR Check

on:
  pull_request:   # Se ejecuta en cualquier PR

jobs:
  test:
    name: Test PR Check
    runs-on: ubuntu-latest
    steps:
      - name: Show PR info
        run: |
          echo "Base branch: ${{ github.base_ref }}"
          echo "Head branch: ${{ github.head_ref }}"
          echo "Este check sirve para que aparezca en la lista de status checks"
